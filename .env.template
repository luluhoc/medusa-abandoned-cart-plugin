# Only needed inside the *plugin project* to run `yarn medusa plugin:db:generate`.
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=medusa-abandoned-cart-plugin

# Read by the scheduled job in the *Medusa application* (see README).
# Cron expression for the abandoned-cart sweep. Defaults to every 15 minutes.
ABANDONED_CART_CRON=*/15 * * * *
